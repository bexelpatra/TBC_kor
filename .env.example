# docker-compose 용 환경변수 — 이 파일을 .env 로 복사해 실제 값을 채운다.
# .env 는 .gitignore 에 있으므로 절대 커밋되지 않는다.

# PostgreSQL (컨테이너 초기화 + API 접속에 모두 사용)
POSTGRES_USER=admin
POSTGRES_PASSWORD=
POSTGRES_DB=brain_core_kor

# 프론트엔드 빌드 시 주입되는 백엔드 API 주소
VITE_API_BASE=http://localhost:8088
