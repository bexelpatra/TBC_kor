server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Vue Router history mode: SPA 라우트는 모두 index.html로 폴백
    location / {
        try_files $uri $uri/ /index.html;
    }
}
